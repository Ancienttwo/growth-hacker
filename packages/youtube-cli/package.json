{
  "name": "@growth-hacker/youtube-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "yt-cli": "./bin/yt-cli"
  },
  "scripts": {
    "start": "bun src/cli.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  }
}
